[pytest]
addopts = -q --tb=short
testpaths = store/code
markers =
    slow: marks tests that take longer than a few seconds (deselect with '-m "not slow"')
    integration: marks tests that require external services (IBKR, TWS, etc.)
