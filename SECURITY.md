# Security Policy

- Never commit secrets. Use `.env` locally; keep `.env` in `.gitignore`.
- Rotate any credential accidentally exposed.
- Report sensitive issues privately to the maintainer via GitHub Security Advisories.
