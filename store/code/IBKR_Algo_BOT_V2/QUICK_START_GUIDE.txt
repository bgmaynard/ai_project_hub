╔══════════════════════════════════════════════════════════════════════╗
║                                                                      ║
║            🎯 QUICK START - CONTINUATION GUIDE 🎯                   ║
║                Ready to Deploy & Test                                ║
║                                                                      ║
╚══════════════════════════════════════════════════════════════════════╝


═══════════════════════════════════════════════════════════════════════
                    🚀 INSTANT DEPLOYMENT - OPTION 1
═══════════════════════════════════════════════════════════════════════

Use the Automated Script (Recommended):

1. Open PowerShell as Administrator

2. Navigate to project:
   cd C:\ai_project_hub\store\code\IBKR_Algo_BOT_V2

3. Run deployment script:
   .\DEPLOY_COMPLETE_API.ps1

   Or if script is in Downloads:
   C:\Users\bgmay\Downloads\DEPLOY_COMPLETE_API.ps1

4. Follow prompts:
   - Script will backup your current API
   - Deploy the complete version
   - Start server automatically
   - Test all endpoints
   - Connect to IBKR (if TWS is running)

5. Open platform:
   http://127.0.0.1:9101/ui/platform.html


What the Script Does Automatically:
────────────────────────────────────────────────────────────────────────
✅ Stops any running server
✅ Backs up current dashboard_api.py
✅ Deploys dashboard_api_COMPLETE.py
✅ Sets up .env with API key
✅ Starts server on port 9101
✅ Tests health endpoint
✅ Connects to IBKR (with prompt)
✅ Subscribes to AAPL, TSLA, SPY
✅ Tests all critical endpoints
✅ Shows you the final URLs


═══════════════════════════════════════════════════════════════════════
                    🔧 MANUAL DEPLOYMENT - OPTION 2
═══════════════════════════════════════════════════════════════════════

If you prefer manual control:

STEP 1: Navigate to Project
────────────────────────────────────────────────────────────────────────
cd C:\ai_project_hub\store\code\IBKR_Algo_BOT_V2


STEP 2: Stop Current Server
────────────────────────────────────────────────────────────────────────
# If running, press Ctrl+C in the server terminal
# Or find and stop the process:
Get-Process python | Where-Object {$_.CommandLine -like "*dashboard_api*"} | Stop-Process


STEP 3: Backup & Deploy
────────────────────────────────────────────────────────────────────────
# Backup current file
Copy-Item "dashboard_api.py" "dashboard_api.backup_$(Get-Date -Format 'yyyyMMdd_HHmmss').py"

# Deploy complete version
Copy-Item "C:\Users\bgmay\Downloads\dashboard_api_COMPLETE.py" "dashboard_api.py" -Force


STEP 4: Set API Key
────────────────────────────────────────────────────────────────────────
$env:ANTHROPIC_API_KEY = "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"


STEP 5: Start Server
────────────────────────────────────────────────────────────────────────
python dashboard_api.py


STEP 6: Connect to IBKR (New Terminal)
────────────────────────────────────────────────────────────────────────
# Make sure TWS Paper Trading is running on port 7497

$connectBody = @{
    host = "127.0.0.1"
    port = 7497
    client_id = 1
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://127.0.0.1:9101/api/ibkr/connect" -Method POST -ContentType "application/json" -Body $connectBody


STEP 7: Subscribe to Symbols
────────────────────────────────────────────────────────────────────────
# Subscribe to AAPL
$subscribeBody = @{
    symbol = "AAPL"
    exchange = "SMART"
    data_types = @("quote")
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://127.0.0.1:9101/api/subscribe" -Method POST -ContentType "application/json" -Body $subscribeBody

# Repeat for TSLA, SPY, QQQ, etc.


STEP 8: Test & Verify
────────────────────────────────────────────────────────────────────────
# Health check
Invoke-RestMethod -Uri "http://127.0.0.1:9101/health"

# Test old endpoints (platform.html compatibility)
Invoke-RestMethod -Uri "http://127.0.0.1:9101/api/price/AAPL"
Invoke-RestMethod -Uri "http://127.0.0.1:9101/api/bot/status"

# Test Claude with real market data
Invoke-RestMethod -Uri "http://127.0.0.1:9101/api/claude/analyze-with-data/AAPL"


STEP 9: Open Platform
────────────────────────────────────────────────────────────────────────
http://127.0.0.1:9101/ui/platform.html


═══════════════════════════════════════════════════════════════════════
                    🎯 WHAT TO EXPECT AFTER DEPLOYMENT
═══════════════════════════════════════════════════════════════════════

Health Check Should Show:
────────────────────────────────────────────────────────────────────────
{
  "status": "healthy",
  "ibkr_connected": true,          ✅ Connected to IBKR
  "ibkr_available": true,
  "ai_predictor_loaded": false,    ⚠️  Not trained yet (normal)
  "claude_available": true,        ✅ Claude API working
  "active_subscriptions": 3,       ✅ Symbols subscribed
  "symbols_tracked": ["AAPL", "TSLA", "SPY"]
}


Claude Analysis Should Show:
────────────────────────────────────────────────────────────────────────
{
  "symbol": "AAPL",
  "market_data": {
    "last": 226.50,               ✅ Real IBKR data
    "bid": 226.48,
    "ask": 226.52,
    "volume": 45234567,
    "timestamp": "2025-11-13T..."
  },
  "analysis": "AAPL is showing bullish momentum...",  ✅ Claude insight
  "data_source": "IBKR Live"      ✅ Using live data
}


Platform.html Should Show:
────────────────────────────────────────────────────────────────────────
✅ Real-time price updates
✅ Level 2 market depth
✅ Time & Sales feed
✅ Claude analysis panel
✅ No more 404 errors
✅ All modules functioning


═══════════════════════════════════════════════════════════════════════
                    🔥 AFTER IT'S WORKING - NEXT STEPS
═══════════════════════════════════════════════════════════════════════

PRIORITY 1: Train AI Predictor (Optional but Powerful)
────────────────────────────────────────────────────────────────────────
Open Python console:
python

Then run:
from ai.ai_predictor import get_predictor
predictor = get_predictor()
predictor.train("SPY", period="2y")  # Takes 5-10 minutes
predictor.train("AAPL", period="1y")
predictor.train("TSLA", period="1y")

This enables ML predictions alongside Claude analysis.


PRIORITY 2: Configure Pre-Market Scanner
────────────────────────────────────────────────────────────────────────
The system includes a scanner for gap & go setups.

Test it:
http://127.0.0.1:9101/api/scanner/results

Configure scanner criteria in dashboard_api.py:
- Min gap percent: 3%
- Min volume: 500K
- Price range: $5-$50
- News catalyst required


PRIORITY 3: Add More Symbols to Watchlist
────────────────────────────────────────────────────────────────────────
Subscribe to your favorite tickers:

$symbols = @("QQQ", "IWM", "AMD", "NVDA", "META")

foreach ($symbol in $symbols) {
    $body = @{
        symbol = $symbol
        exchange = "SMART"
        data_types = @("quote")
    } | ConvertTo-Json
    
    Invoke-RestMethod -Uri "http://127.0.0.1:9101/api/subscribe" -Method POST -ContentType "application/json" -Body $body
    Start-Sleep -Milliseconds 500
}


PRIORITY 4: Test Auto-Trading (Paper Trading Only!)
────────────────────────────────────────────────────────────────────────
⚠️  IMPORTANT: Only use with Paper Trading account!

The system includes auto_trader module with:
- 3-5-7 risk management (3% per trade, 5% daily limit, 7% max drawdown)
- Multiple strategy support
- Position sizing
- Stop losses

DO NOT enable auto-trading until thoroughly tested!


PRIORITY 5: Integrate TradingView Webhooks
────────────────────────────────────────────────────────────────────────
Your Pine Script strategies can send signals to:
http://127.0.0.1:9101/webhook/tradingview

Endpoint accepts:
{
    "strategy": "Gap and Go",
    "action": "buy",
    "symbol": "AAPL",
    "price": 226.50,
    "timestamp": "2025-11-13T09:30:00"
}


═══════════════════════════════════════════════════════════════════════
                    📊 SYSTEM ARCHITECTURE OVERVIEW
═══════════════════════════════════════════════════════════════════════

Your Algo Bot V2 Architecture:

┌─────────────────────────────────────────────────────────────────────┐
│                         FRONTEND LAYER                              │
│  platform.html → Real-time dashboard with all modules               │
└────────────────────────────┬────────────────────────────────────────┘
                             │
┌────────────────────────────▼────────────────────────────────────────┐
│                      API LAYER (Port 9101)                          │
│  FastAPI Server → REST endpoints + WebSocket streaming              │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
┌───────────────▼──────────┐  ┌──────────▼─────────────────┐
│    MARKET DATA BUS       │  │     AI MODULES             │
│  - IBKR real-time data   │  │  - Claude API (analysis)   │
│  - Level 2 depth         │  │  - LightGBM (predictions)  │
│  - Time & Sales          │  │  - Market Analyst          │
│  - Scanner results       │  │  - Trade Validator         │
└──────────────────────────┘  │  - Alpha Fusion            │
                              │  - Backtester              │
                              └────────────────────────────┘


Data Flow:
──────────────────────────────────────────────────────────────────────
1. IBKR sends market data → Data Bus
2. Data Bus broadcasts to WebSocket clients
3. Platform.html receives real-time updates
4. User queries Claude → API fetches market data → Claude analyzes
5. AI predictions generated → Logged for backtesting
6. Scanner detects setups → Alerts generated
7. (Optional) Auto-trader executes based on signals


═══════════════════════════════════════════════════════════════════════
                    🐛 TROUBLESHOOTING QUICK FIXES
═══════════════════════════════════════════════════════════════════════

PROBLEM: Server won't start
────────────────────────────────────────────────────────────────────────
SOLUTION:
1. Check if port 9101 is in use:
   Get-NetTCPConnection -LocalPort 9101
   
2. Kill process using port:
   $pid = (Get-NetTCPConnection -LocalPort 9101).OwningProcess
   Stop-Process -Id $pid -Force

3. Verify API key is set:
   echo $env:ANTHROPIC_API_KEY


PROBLEM: IBKR won't connect
────────────────────────────────────────────────────────────────────────
SOLUTION:
1. Verify TWS is running and logged in
2. Check TWS API Settings:
   - Global Configuration → API → Settings
   - Enable ActiveX and Socket Clients: ✅
   - Socket Port: 7497 (Paper Trading)
   - Trusted IP: 127.0.0.1
3. Try different client_id (999 instead of 1)
4. Restart TWS


PROBLEM: Claude says "no market data"
────────────────────────────────────────────────────────────────────────
SOLUTION:
1. Make sure you deployed dashboard_api_COMPLETE.py
2. Subscribe to symbol first:
   Invoke-RestMethod -Uri "http://127.0.0.1:9101/api/subscribe" ...
3. Use correct endpoint:
   /api/claude/analyze-with-data/{symbol}  (not /api/claude/chat)
4. Wait 2-3 seconds after subscribing for data to arrive


PROBLEM: Platform.html shows 404 errors
────────────────────────────────────────────────────────────────────────
SOLUTION:
The COMPLETE API has old endpoint compatibility built in.
If still seeing 404s:
1. Verify you deployed dashboard_api_COMPLETE.py
2. Check browser console for actual URLs being called
3. Try clearing browser cache (Ctrl+Shift+R)


PROBLEM: WebSocket connection fails
────────────────────────────────────────────────────────────────────────
SOLUTION:
1. Check firewall isn't blocking port 9101
2. Verify server is running: Invoke-RestMethod "http://127.0.0.1:9101/health"
3. Try connecting via ws://127.0.0.1:9101/ws/market-data


PROBLEM: Import errors for AI modules
────────────────────────────────────────────────────────────────────────
SOLUTION:
1. Check all files exist in ai/ folder:
   Get-ChildItem ai\*.py
2. Verify __init__.py exists:
   Test-Path ai\__init__.py
3. Reinstall dependencies:
   pip install -r requirements.txt --upgrade


═══════════════════════════════════════════════════════════════════════
                    📞 USEFUL COMMANDS CHEAT SHEET
═══════════════════════════════════════════════════════════════════════

Navigate to project:
cd C:\ai_project_hub\store\code\IBKR_Algo_BOT_V2

Start server:
python dashboard_api.py

Set API key (current session):
$env:ANTHROPIC_API_KEY = "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"

Check health:
Invoke-RestMethod -Uri "http://127.0.0.1:9101/health"

Connect IBKR:
$body = @{host="127.0.0.1";port=7497;client_id=1} | ConvertTo-Json
Invoke-RestMethod -Uri "http://127.0.0.1:9101/api/ibkr/connect" -Method POST -ContentType "application/json" -Body $body

Subscribe symbol:
$body = @{symbol="AAPL";exchange="SMART";data_types=@("quote")} | ConvertTo-Json
Invoke-RestMethod -Uri "http://127.0.0.1:9101/api/subscribe" -Method POST -ContentType "application/json" -Body $body

Get price:
Invoke-RestMethod -Uri "http://127.0.0.1:9101/api/price/AAPL"

Claude analysis:
Invoke-RestMethod -Uri "http://127.0.0.1:9101/api/claude/analyze-with-data/AAPL"

Scanner results:
Invoke-RestMethod -Uri "http://127.0.0.1:9101/api/scanner/results"

View logs in browser:
http://127.0.0.1:9101/docs (FastAPI auto-docs)

Open platform:
http://127.0.0.1:9101/ui/platform.html


═══════════════════════════════════════════════════════════════════════
                    🎯 SUCCESS CRITERIA - YOU'LL KNOW IT WORKS WHEN:
═══════════════════════════════════════════════════════════════════════

✅ Health endpoint returns ibkr_connected: true
✅ Health endpoint returns claude_available: true
✅ /api/price/AAPL returns real-time price data
✅ /api/bot/status returns current system status
✅ Claude analysis includes actual market data in response
✅ Platform.html loads without 404 errors
✅ WebSocket shows real-time price updates
✅ Level 2 shows bid/ask depth
✅ Time & Sales shows recent trades
✅ Scanner finds pre-market gappers


═══════════════════════════════════════════════════════════════════════
                    🚀 YOU'RE READY TO GO!
═══════════════════════════════════════════════════════════════════════

Choose your deployment method:
1. Automated: Run DEPLOY_COMPLETE_API.ps1
2. Manual: Follow Step-by-Step guide above

After deployment, test everything and enjoy your fully integrated
IBKR Algorithmic Trading Bot with Claude AI analysis!

Next chat: "Everything's working! What's next?" or "Having an issue with X"

Good luck! 🎯
